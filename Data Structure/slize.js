function forecast(arr) {
  // change code below this line
  var todayWeather=arr.slice(2,4);
  return todayWeather;
}

// do not change code below this line
console.log(forecast(['cold', 'rainy', 'warm', 'sunny', 'cool', 'thunderstorms']));
